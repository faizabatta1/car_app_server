<!DOCTYPE html>
<html>
<head>
    <title>Gruppeliste</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .controls{
            display: flex;
        }

        .controls > *{
            flex-grow: 1;
        }
    </style>
</head>
<body>
<%- include('../components/navbar') %>
<div class="container mt-4">
    <div class="row">
        <div class="col">
            <h1>Gruppeliste</h1>
        </div>
        <div class="col text-right">
            <a href="/groups/create" class="btn btn-primary">Opprett ny gruppe</a>
        </div>
    </div>
    <hr>

    <div class="row">
        <div class="col">
            <table class="table table-bordered table-striped text-center table-hover">
                <thead>
                <tr>
                    <th>Navn</th>
                    <th>Merke</th>
                    <th>Tekst</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                <% groups.forEach(group => { %>
                    <tr>
                        <td><%= group.name %></td>
                        <td><%= group.notice %></td>
                        <td><%= group.text %></td>
                        <td class="controls">
                            <a href="/groups/<%= group._id %>/edit" class="btn btn-primary btn-sm mr-2">Redigere</a>
                            <form action="/groups/<%= group._id %>?_method=DELETE" method="POST" style="display: inline;">
                                <button type="submit" class="btn btn-danger btn-sm w-100">Slett</button>
                            </form>
                        </td>
                    </tr>
                <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</div>
</body>
</html>
