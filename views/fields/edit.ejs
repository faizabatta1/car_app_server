<!DOCTYPE html>
<html>
<head>
    <title>Edit Form Field</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
<div class="container">
    <h1>Edit Form Field</h1>
    <form action="/api/formFields/<%= formField._id %>?_method=PUT" method="POST">
        <div class="form-group">
            <label for="title">Title</label>
            <input type="text" class="form-control" id="title" name="title" value="<%= formField.title %>" required>
        </div>
        <div class="form-group">
            <label for="answerDataType">Answer Data Type</label>
            <select class="form-control" id="answerDataType" name="answerDataType" required>
                <option value="text" <%= formField.answerDataType === 'text' ? 'selected' : '' %>>Text</option>
                <option value="yes_no" <%= formField.answerDataType === 'yes_no' ? 'selected' : '' %>>Yes-No</option>
                <option value="file" <%= formField.answerDataType === 'file' ? 'selected' : '' %>>Files</option>
            </select>
        </div>
        <div class="form-group form-check">
            <input type="checkbox" class="form-check-input" id="hasRequiredDescription" name="hasRequiredDescription" <%= formField.hasRequiredDescription ? 'checked' : '' %>>
            <label class="form-check-label" for="hasRequiredDescription">Has Required Description</label>
        </div>
        <div class="form-group">
            <label for="requiredDescription">Required Description</label>
            <input type="text" class="form-control" id="requiredDescription" name="requiredDescription" value="<%= formField.requiredDescription %>">
        </div>
        <button type="submit" class="btn btn-primary">Update</button>
    </form>
</div>
</body>
</html>
