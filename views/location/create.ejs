<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Location</title>
    <!-- Add Bootstrap CSS link -->
    <style>
        .checkbox-group {
            display: flex;
            flex-direction: column;
        }

        .checkbox-label {
            display: flex;
            align-items: center;
            margin-bottom: 5px;
        }

        .checkbox-label input[type="checkbox"] {
            margin-right: 5px;
        }
    </style>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
<%- include('../components/navbar') %>

<div class="container mt-5">
    <h1 class="text-center">Add New Location</h1>
    <div class="row justify-content-center">
        <div class="col-md-6">
            <form id="newLocationForm">
                <div class="form-group">
                    <label for="location">Location</label>
                    <input type="text" class="form-control" id="location" name="location" required>
                </div>
                <div class="d-flex justify-content-between">
                    <div class="form-group">
                        <label>Choose Days</label>
                        <div class="checkbox-group">
                            <label class="checkbox-label"><input type="checkbox" name="days[]" value="saturday"> Saturday</label>
                            <label class="checkbox-label"><input type="checkbox" name="days[]" value="sunday"> Sunday</label>
                            <label class="checkbox-label"><input type="checkbox" name="days[]" value="monday"> Monday</label>
                            <label class="checkbox-label"><input type="checkbox" name="days[]" value="tuesday"> Tuesday</label>
                            <label class="checkbox-label"><input type="checkbox" name="days[]" value="wednesday"> Wednesday</label>
                            <label class="checkbox-label"><input type="checkbox" name="days[]" value="thursday"> Thursday</label>
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Choose Shift</label>
                        <div class="checkbox-group">
                            <label class="checkbox-label"><input type="checkbox" name="shifts[]" value="day"> Day</label>
                            <label class="checkbox-label"><input type="checkbox" name="shifts[]" value="night"> Night</label>
                            <label class="checkbox-label"><input type="checkbox" name="shifts[]" value="weekend"> Weekend</label>
                        </div>
                    </div>
                </div>

                <div class="text-center">
                    <button type="button" class="btn btn-primary" onclick="createLocation()">Add Location</button>
                    <a href="/locations" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>
<!-- Add Bootstrap JS and Popper.js scripts -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/2.10.2/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

<script>
    function createLocation() {
        const shifts = document.querySelectorAll('input[name="shifts[]"]');
        const days = document.querySelectorAll('input[name="days[]"]');

        const selectedShifts = [];
        shifts.forEach(function (checkbox) {
            if (checkbox.checked) {
                selectedShifts.push(checkbox.value);
            }
        });

        const selectedDays = [];
        days.forEach(function (checkbox) {
            if (checkbox.checked) {
                selectedDays.push(checkbox.value);
            }
        });



        const location = document.querySelector('#location')
        fetch('/api/locations', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({
                location: location.value,
                shifts: selectedShifts,
                days: selectedDays
            })
        })
            .then(response => {
                if (response.status == 200) {
                    alert('Location added successfully.');
                    window.location.href = '/locations'; // Redirect to the location list page
                } else {
                    alert('Failed to add location. Please try again.');
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('An error occurred while adding the location. Please try again.');
            });
    }
</script>
</body>
</html>
